import { StyleSheet, Text, View } from "react-native"

const styles = StyleSheet.create({
    container: {
        marginTop: 40,
    }
})
const Flexbox = () => {
    return (
        <View style>
            <Text>flexbox</Text>
        </View>
    )
}

export default Flexbox